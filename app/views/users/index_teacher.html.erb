<% provide(:title, 'Teachers') %>
<h1>Current Teachers</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <% if user.teacher? %>
    <%= render user %>
    <% end %>
  <% end %>
</ul>

<%= will_paginate %>
