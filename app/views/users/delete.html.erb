<% provide(:title, "Delete User") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  
  <%= form_for(@users, :method => 'delete') do |f| %>
  <h1> Are you sure you wish to perminently delete this User? </h1>
  <%= f.submit "Delete User", class: "btn btn-danger" %>
  <% end %>
  
  </div>
</div>
