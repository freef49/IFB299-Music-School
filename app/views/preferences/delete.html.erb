<% provide(:title, "Delete Preference") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  
  <%= form_for(@preferences, :method => 'delete') do |f| %>
  <h1> Are you sure you wish to perminently delete this Preference? </h1>
  <%= f.submit "Delete Preference", class: "btn btn-danger" %>
  <% end %>
  
  </div>
</div>