<% provide(:title, "Delete Booking") %>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
  
  <%= form_for(@free_play_rooms, :method => 'delete') do |f| %>
  <h1> Are you sure you wish to perminently delete this booking? </h1>
  <%= f.submit "Delete Booking", class: "btn btn-danger" %>
  <% end %>
  
  </div>
</div>
